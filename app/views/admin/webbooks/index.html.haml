%h2 管理者用トップページ

.card-group
  -@webbooks.each do |webbook|
    .col-md-4{style: "margin-bottom: 30px"}
      .card
        .row.no-gutters
          .col-md-6
            .bd-placeholder-img
              = image_tag webbook.image.variant(resize_to_fit: [160, 160]) if webbook.image.attached?
          .col-md-6
            .card-body
              .card-title
                %h5= link_to webbook.title, admin_webbook_path(webbook)
              .card-text
                %p= webbook.author
              .card-text
                %p= "#{webbook.release_date}発売"
              .card-text
                %h5= "#{webbook.price}円"
= link_to '新しい本を追加する', new_admin_webbook_path, class: 'btn btn-primary mr-3'
= link_to '購入履歴を確認する', admin_purchase_histories_path, class: 'btn btn-primary mr-3'